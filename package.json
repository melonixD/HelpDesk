{
  "name": "helpdesk-hbtu",
  "version": "19.0.2",
  "private": true,
  "description": "HelpDesk — a premium, Netlify-ready resource library for HBTU students.",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/prepare-netlify.js",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.3",
    "bcryptjs": "^3.0.3"
  }
}
